# Copiar para .env ao lado do compose lab se precisares de override.
KAFKA_HOST_PORT=9092
# Brokers para app local (quando KAFKA_ENABLED=1):
# KAFKA_BROKERS=localhost:9092
# KAFKA_CLIENT_ID=genesis-app-local
